var Fs = require('fs');

var writeStream = Fs.createWriteStream('./test/fixtures/wtf.log', { flags: 'a'});

setInterval(function() {
    writeStream.write('Data at' + Date.now() + '\n', function () {

        console.log(writeStream._writableState.buffer.length);
    });
    writeStream.emit('error' , new Error('seeing what happens'));
}, 10);