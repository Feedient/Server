/**
 * PanelType Enum, every value is a bit
 * @type {Object}
 */
var PanelType = {
    FEED:           0,
    PAGES:          1,
    INBOX:          2,
    ANALYTICS:      4,
    NOTIFICATION:   8
};

module.exports = PanelType;
